package com.example.highcash.ui.views;

import android.content.Context;
import android.content.res.ColorStateList;
import android.graphics.drawable.Drawable;

import androidx.appcompat.content.res.AppCompatResources;
import androidx.core.graphics.drawable.DrawableCompat;

import com.example.highcash.R;

public class NavigationViewItemBackground {

    /*
    public static Drawable navigationItemBackground(Context context) {
        // Need to inflate the drawable and CSL via AppCompatResources to work on Lollipop
        Drawable background =
                AppCompatResources.getDrawable(context, R.drawable.navigation_item_bg);
        if (background != null) {
             ColorStateList tint = AppCompatResources.getColorStateList(
                    context, R.color.navigation_item_background_tint);
            background = DrawableCompat.wrap(background.mutate());
            background.setTintList(tint);
        }
        return background;
    }

     */
}
